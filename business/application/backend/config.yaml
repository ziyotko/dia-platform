server:
  port: 8087
  mode: debug
  api_prefix: /business_application/api
  # 上传文件挂载前缀：实际挂在 <upload_dir_prefix>/uploads（与前端 VITE_BASE_PATH 去尾斜杠一致）
  upload_dir_prefix: /business_application
  max_concurrent_ips: 100
  # 可信反向代理 IP 列表（用于正确获取客户端真实 IP；限流/审计均按真实 IP 计数）
  # 生产环境请填入 Nginx 等代理的实际地址；本机回环保留 127.0.0.1。
  trusted_proxies:
    - 127.0.0.1

mysql:
  host: 10.3.1.95
  port: 63400
  user: root
  password: '1qaz@WSX'
  db_name: caam_application
  charset: utf8mb4
  max_open: 50
  max_idle: 10

redis:
  addr: 127.0.0.1:6379
  password:
  captcha_db: 2
  anti_replay_db: 3

jwt:
  secret: application-jwt-secret-key-2026
  expire_hours: 24
  issuer: caam-application

log:
  path: logs/application.log
  max_size: 100
  max_backups: 30
  max_age: 180
